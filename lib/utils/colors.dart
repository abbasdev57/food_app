import 'dart:ui';

/// A centralized class for managing all the app's color constants.

class AppColors {
  /// The default text color used for general body text.
  static const Color textColor = Color(0xFFccc7c5);

  /// The primary/main color of the app (used for themes, highlights, buttons).
  static const Color mainColor = Color(0xFF89dad0);

  /// Color used for prominent titles/headings.
  static const Color titleColor = Color(0xFF5c524f);

  /// Dark color used for main text or bold UI elements.
  static const Color mainBlackColor = Color(0xFF332d2b);

  /// Light orange used for icons and decorative elements.
  static const Color iconColor1 = Color(0xFFffd28d);

  /// Light pinkish-orange used as a secondary icon color.
  static const Color iconColor2 = Color(0xFFfcab88);

  /// Grayish color used for paragraph/subtitle text.
  static const Color paraColor = Color(0xFF8f837f);

  /// Background color for buttons or containers.
  static const Color btnBgColor = Color(0xffe3dfd7);

  /// Color used for secondary/subdued text (e.g. sign-in, labels).
  static const Color signColor = Color(0xFFa9a29f);

  /// Yellow tone used for ratings, highlights, etc.
  static const Color yellowColor = Color(0xFFffd379);
}